snapshots:
  - name: hosts_full_data
    relation: ref('silver_hosts')
    config:
      schema: gold
      database: SNOWFLAKE_DBT
      unique_key: HOST_ID
      strategy: timestamp
      updated_at: CREATED_AT
      dbt_valid_to_current: "to_date('9999-12-31')"
